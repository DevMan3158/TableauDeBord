{% extends 'base.html.twig' %}

{% block title %}Product{% endblock %}

{% block body %}
    <h1>Product</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ product.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ product.Name }}</td>
            </tr>
            <tr>
                <th>Id_cat</th>
                <td>{{ product.idCat }}</td>
            </tr>
            <tr>
                <th>Lieux_achat</th>
                <td>{{ product.lieuxAchat }}</td>
            </tr>
            <tr>
                <th>Date_achat</th>
                <td>{{ product.dateAchat ? product.dateAchat|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Date_fin_garantie</th>
                <td>{{ product.dateFinGarantie }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ product.prix }}€</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ product.description }}</td>
            </tr>
            <tr>
                <th>Photo</th>
                <td>{{ product.photo }}</td>
            </tr>
            
        </tbody>
    </table>

    <a href="{{ path('app_products_index') }}">back to list</a>

    <a href="{{ path('app_products_edit', {'id': product.id}) }}">edit</a>

    {{ include('products/_delete_form.html.twig') }}
{% endblock %}
